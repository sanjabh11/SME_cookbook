# Image-to-Prototype ROS2 Generator

Welcome to the Robotics workflow. This directory helps convert visual mockups into working ROS2 boilerplate code.

## Usage Instructions

1. **Review Input Data:** You can drop an actual image file here or use the description in `sample_mockup.txt`.
2. **Open Antigravity/Claude Code:** Inside this folder.
3. **Run the Prompt:** Type the following exact prompt:
   > "Create image-to-prototype workflow: paste mockup → generate ROS2 Python node + simulation. Use `sample_mockup.txt` as the reference mockup."
4. **Auto-accept:** Remember to use `Shift+Tab` to auto-accept your task steps if safe.
5. **Git Checkpoint:** The assistant knows to checkpoint with Git before doing large file generations.
